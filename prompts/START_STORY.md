# Prompt: Start the next work item

```text
Read AGENTS.md and backlog/PROGRESS.md.

Select the first unblocked item from docs/IMPLEMENTATION_ORDER.md, unless I name
a different item. Read only that item and its context_docs.

Before coding:
- verify dependencies are done
- move the item to backlog/in-progress/
- update its status/frontmatter and backlog/PROGRESS.md
- provide a plan with at most 8 bullets

Implement only this item. Run targeted checks during development and the final
checks required by its verification field. Fill the Result section and move it
to done only when every acceptance criterion is met.
```
